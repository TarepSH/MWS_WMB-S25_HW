{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "npx prisma generate --schema prisma/schema.local.prisma",
    "prisma:migrate": "npx prisma migrate dev --schema prisma/schema.local.prisma",
    "db:seed": "node prisma/seed.js",
    "db:reset": "npx prisma migrate reset --force --schema prisma/schema.local.prisma",
    "prisma:generate:pg": "npx prisma generate --schema prisma/schema.prisma",
    "prisma:migrate:pg": "npx prisma migrate dev --schema prisma/schema.prisma",
    "db:reset:pg": "npx prisma migrate reset --force --schema prisma/schema.prisma",
    "smoke:test": "node scripts/smoke-test.js",
    "test": "echo \"No tests\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prisma": "^6.12.0"
  }
}
